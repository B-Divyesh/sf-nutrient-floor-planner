{
  "name": "nutrient-floor-planner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build && node scripts/generate-sw.mjs",
    "preview": "vite preview",
    "test": "node scripts/check-copy-audit.mjs && vitest run",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.11.0",
    "@playwright/test": "1.58.2",
    "typescript": "~5.7.2",
    "vite": "~6.4.3",
    "vitest": "~3.2.7"
  }
}
